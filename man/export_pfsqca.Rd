% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_pfsqca.R
\name{export_pfsqca}
\alias{export_pfsqca}
\title{Export Panel fsQCA Results to CSV Files}
\usage{
export_pfsqca(x, dir = "pfsqca_results", quiet = FALSE)
}
\arguments{
\item{x}{A \code{pfsqca_result} object from \code{\link[=run_pfsqca]{run_pfsqca()}}.}

\item{dir}{Character. Folder to write into. Created if it does not exist.
Default is "pfsqca_results".}

\item{quiet}{Logical. If TRUE, suppresses the progress message.}
}
\value{
Invisibly, the character vector of files written.
}
\description{
Writes every result table of a \code{\link[=run_pfsqca]{run_pfsqca()}} analysis to a folder as CSV
files, plus a plain-text record of the parameters used. Handy for opening
results in Excel or attaching them to a paper's replication package.
}
\examples{
\donttest{
res <- run_pfsqca(
  example_panel, "entrepreneurship",
  c("infrastructure", "knowledge", "finance", "talent"),
  params = pfsqca_params(incl_cut = 0.80, n_cut = 1),
  robustness = FALSE, verbose = FALSE
)
out <- file.path(tempdir(), "results")
export_pfsqca(res, dir = out)
}

}
