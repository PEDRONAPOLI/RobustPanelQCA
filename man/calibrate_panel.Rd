% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibration.R
\name{calibrate_panel}
\alias{calibrate_panel}
\title{Calibrate Multiple Variables in a Data Frame}
\usage{
calibrate_panel(data, vars, probs = c(0.1, 0.5, 0.9))
}
\arguments{
\item{data}{A data frame containing the variables to calibrate.}

\item{vars}{Character vector with names of variables to calibrate.}

\item{probs}{Numeric vector of length 3 with percentiles for
full exclusion, crossover, and full inclusion. Default is c(0.10, 0.50, 0.90).}
}
\value{
A data frame with calibrated variables (original values replaced).
}
\description{
Calibrates multiple variables in a data frame to fuzzy-set membership
scores using percentile-based thresholds.
}
\details{
Calibration is applied to the pooled panel: all case-period observations of
a variable share the same anchors, so a membership score of 0.8 means the
same thing in period 1 and in period 3. Columns not listed in \code{vars}
(identifiers, time, labels) are carried through untouched.
}
\examples{
conditions <- c("infrastructure", "knowledge", "finance", "talent")
data_cal <- calibrate_panel(
  example_panel,
  vars = c(conditions, "entrepreneurship")
)
head(data_cal)

}
