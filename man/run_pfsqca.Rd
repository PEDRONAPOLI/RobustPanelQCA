% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_pfsqca.R
\name{run_pfsqca}
\alias{run_pfsqca}
\title{Run the Complete Panel fsQCA Workflow}
\usage{
run_pfsqca(
  data,
  outcome,
  conditions,
  id_var = "case_id",
  time_var = "period",
  params = NULL,
  calibrate = TRUE,
  probs = c(0.1, 0.5, 0.9),
  robustness = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{data}{A data frame in long panel format: one row per case-period, one
column per condition, plus a case identifier and a time column.}

\item{outcome}{Character. Name of the outcome column.}

\item{conditions}{Character vector. Names of the condition columns.}

\item{id_var}{Character. Name of the case identifier column.
Default is "case_id".}

\item{time_var}{Character. Name of the time column. Default is "period".}

\item{params}{A \code{pfsqca_params} object. If NULL, uses \code{\link[=pfsqca_params]{pfsqca_params()}}
defaults.}

\item{calibrate}{Logical. If TRUE (default), raw values are calibrated with
\code{\link[=calibrate_panel]{calibrate_panel()}}. Set to FALSE when your data already holds fuzzy-set
scores in the 0-1 range.}

\item{probs}{Numeric vector of length 3. Calibration percentiles passed to
\code{\link[=calibrate_panel]{calibrate_panel()}}. Ignored when \code{calibrate = FALSE}.}

\item{robustness}{Logical. If TRUE (default), runs the random deletion
robustness tests. These are the slowest part of the analysis; set to FALSE
for a quick first look.}

\item{verbose}{Logical. If TRUE (default), prints progress messages.}
}
\value{
An object of class \code{pfsqca_result}: a list with the analysis inputs
(\code{data_raw}, \code{data_cal}, \code{params}, \code{outcome}, \code{conditions}, \code{id_var},
\code{time_var}) and every result table (\code{necessity}, \code{necessity_panel},
\code{necessity_robustness}, \code{sufficiency}, \code{terms}, \code{panel_metrics},
\code{coverage}, \code{sufficiency_robustness}, \code{ball_table}, \code{solution_type}).
}
\description{
Runs every step of a panel fsQCA analysis in one call: calibration,
necessity, necessity panel diagnostics, necessity robustness, sufficiency
(truth table and minimization), panel metrics, unique coverage, and
sufficiency robustness.

This is the function behind the fill-in-the-blanks script produced by
\code{\link[=use_pfsqca_template]{use_pfsqca_template()}}. If you want to inspect or tweak individual steps,
call the underlying functions directly.
}
\examples{
\donttest{
conditions <- c("infrastructure", "knowledge", "finance", "talent")
res <- run_pfsqca(
  data       = example_panel,
  outcome    = "entrepreneurship",
  conditions = conditions,
  id_var     = "case_id",
  time_var   = "period",
  params     = pfsqca_params(incl_cut = 0.80, n_cut = 1, robustness_B = 50)
)
res
res$panel_metrics$pooled
}

}
\seealso{
\code{\link[=use_pfsqca_template]{use_pfsqca_template()}} to get a ready-to-edit script,
\code{\link[=export_pfsqca]{export_pfsqca()}} to write the tables to CSV, and \code{\link[=pfsqca_report]{pfsqca_report()}} to
render an HTML report.
}
